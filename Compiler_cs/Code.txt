Func NewFunc();	/함수;

Println("Hello World!");	/출력;

EndFunc;

Var A;
A = 0;
Println(A);	
If(A <= 0);	/의문문;

NewFunc();

EndIf;
While(A == 0);	/반복문;

Println("While");	/While문 검사용;
Input(A);	/입력;

EndWhile;
